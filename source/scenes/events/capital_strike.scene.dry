title: 资本罢工
subtitle: 德国的工业正在停摆……
view-if: (capital_strike_progress >= (10+(dvp_relation/100))) and spd_in_government and not in_capital_strike
new-page: true
max-visits: 1
on-arrival: in_capital_strike = 1; unemployed += 5*bourgeois_power; coalition_dissent += 1; coalition_dissent += 1 if in_grand_coalition; economic_growth -= 4*bourgeois_power; socialism -= 7*bourgeois_power; capital_strike_over = 0; {! 
if (Q.weimar_plan_adopted) {
    Q.weimar_dissent += 2;
} 
Q.workers_spd *= (0.5+(Q.socialism/200));
Q.unemployed_spd *= (0.5+(Q.socialism/200));
Q.new_middle_spd -= (15*Q.bourgeois_power);
if (Q.new_middle_conscious) {
    Q.new_middle_spd += 3*(Q.socialism/100);
} 
Q.old_middle_spd -= (15*Q.bourgeois_power); 
Q.rural_spd -= (15*Q.bourgeois_power); 
Q.pro_republic -= (15*Q.bourgeois_power); 
Q.capital_strike_seen = 1; 
Q.z_relation -= 10; 
Q.dvp_relation -= 10; 
if (!Q.capital_nazi) {
    Q.workers_nsdap += (8*Q.bourgeois_power);
    Q.unemployed_nsdap += (10*Q.bourgeois_power);
    Q.new_middle_nsdap += (15*Q.bourgeois_power);
    Q.old_middle_nsdap += (15*Q.bourgeois_power);
    Q.rural_nsdap += (15*Q.bourgeois_power);
    Q.bourgeoisie_nsdap += 30;
}
if (Q.capital_nazi) {
    Q.workers_kpd += (4*(Q.socialism/100));
    Q.workers_nsdap += (2*Q.bourgeois_power);
    if (!Q.sapd_consolidated) {
        Q.workers_kpd += (2*(Q.socialism/100));
    }
    if (Q.sapd_consolidated) {
        Q.workers_sapd += (2*(Q.socialism/100));
    }
    Q.unemployed_kpd += (7*(Q.socialism/100));
    Q.unemployed_nsdap += (2*Q.bourgeois_power);
    Q.unemployed_dnvp += (1*Q.bourgeois_power);
    Q.new_middle_kpd += (1*(Q.socialism/100));
    Q.new_middle_nsdap += (5*Q.bourgeois_power);
    Q.new_middle_dnvp += (9*Q.bourgeois_power);
    Q.old_middle_nsdap += (3*Q.bourgeois_power);
    Q.old_middle_dnvp += (12*Q.bourgeois_power);
    Q.rural_dnvp += (15*Q.bourgeois_power);
    Q.bourgeoisie_dnvp += 20;
}
Q.bourgeois_power -= 0.03;
if (Q.price_controls) {
    Q.black_market += 6;
}

Q.timers.push('capital_strike');
Q.capital_strike_timer = Q.capital_strike_progress;
!} 
tags: event

= Capital Strike

Our policies have greatly angered the capitalists. Investment has fallen, and factories are idling, preventing the workers from fulfilling their jobs. Meanwhile, the bourgeois press is a constant chorus of venomous invective directed at us and our economic policies.

- @revolutionary_approach: 阶级斗争升级。
- @right_approach: 这显然是我们治理的失败。
- @address: 我们能做什么?

@revolutionary_approach
choose-if: left_strength+(center_strength*(socialism/100)) >= 50
subtitle: 社民党的马克思主义战略一贯是:通过社会主义改革挑起与资本家的对抗,而通向社会主义变革的断裂便由此发生。如今,这一断裂的时刻已经到来。
on-arrival: reformist_dissent += 10; left_strength += 10; left_dissent -= 10; class_war += 1; class_war += 1 if left_plan_adopted
go-to: address

The revolutionaries of the party put forward their proposed approach to the crisis. They say that this is a success of our strategies, so long as we take it to the next level and wage the class war from below, just as the businesses now do so from above by seeking to bleed the economy dry.

这一主张尚未被采纳。还需要更多说服工作。

@right_approach
choose-if: reformist_strength+neorevisionist_strength+(labor_strength*(1-(socialism/100))) >= 50
subtitle: 我们的最终目标是改善工人的生活境况。这只能通过建设性的施政来实现,而非通过我们的政策亲手制造的危机。我们失败了,必须补救这一失败。
on-arrival: left_dissent += 10; reformist_strength += 10; reformist_dissent -= 10; reconcile_business += 1; reconcile_business += 1 if wtb_adopted or weimar_plan_adopted
go-to: address

The rightists of the party put forward their proposed approach to the crisis. They say that this is a failure of our strategies, a result of us failing to account for the business community's opinions in our haste to deliver for the workers, and that the transition to socialism must be a gradual process which at all steps benefits the worker, without economic crises.

这一主张尚未被采纳。还需要更多说服工作。

@address
subtitle: 我们必须就眼下的危机本身加以应对。

For the time being, we can make no grander statements as to the character of this crisis. We must focus on the immediate crisis. 

- @seize
- @capital_controls
- @propaganda
- @business
- @nothing

@seize
title: 赋权工人夺取工厂!
unavailable-subtitle: [? if left_strength <= reformist_strength + neorevisionist_strength : 党内多数不支持这一激进举措。 ?] [? if judicial_reform < 3 : 司法系统会反对这一举措。 ?] [? if rb_militancy < 0.25 : 若我们增强准军事力量的实力,或许就能这么做。 ?] [? if works_councils < 1 : 我们需要先得到工厂委员会的支持。 ?][? if left_coalition+sapd_r < 50 : 国会反对。 ?][? if class_war < 1 : 这与我们应对危机的策略相悖。 ?]
choose-if: class_war >= 1 and left_strength > reformist_strength + neorevisionist_strength and (judicial_reform >= 3 or rb_militancy >= 0.25) and works_councils >= 1 and ((left_coalition+sapd_r >= 50) or (rb_force+rfb_force >= 50))
on-arrival: class_war += 1; bourgeois_power -= 0.07; nationalization_progress += 1; coup_progress += 6; unemployed -= 2; unemployed -= 1 if works_councils >= 3; works_program += 1; workers_spd += 10*(1-dissent); unemployed_spd += 10*(1-dissent); coalition_dissent += 2; dvp_relation -= 4; workers_nsdap -= 8*(1-dissent); unemployed_nsdap -= 8*(1-dissent); left_strength += 15; reformist_dissent += 10; left_dissent -= 10; factory_takeovers += 1; economic_growth += 2; socialism += 3; capital_occupations = 1

If the capitalists are going to attack us, then we must hit them back. We have empowered the workers to seize and take full control of the factories that the capitalists have idled. The fascists, of course, call us thieves and looters.


@capital_controls
title: 实施资本管制以减轻冲击。
choose-if: judicial_reform >= 2 and (left_coalition+sapd_r >= 50) or (popular_front+sapd_r >= 50 and z_relation >= 60 or (z_party_leader == "Joos" and z_relation >= 40))
unavailable-subtitle: [? if judicial_reform < 2 : 司法系统绝不会允许此事。 ?][? if judicial_reform >= 2 : 国会反对。 ?]
on-arrival: unemployed -= 1.5; bourgeois_power -= 0.03; workers_spd += 6*(1-dissent); unemployed_spd += 6*(1-dissent); economic_growth += 2; capital_controls = 1

Capital controls might blunt some of the worst economic components of the capital strike.



@propaganda
title: 发动宣传攻势,把危机归咎于资本家。
subtitle: -1 资源
choose-if: (radio >= 2 or commercialized_media >= 3) and resources >= 1
unavailable-subtitle: [? if radio < 2 and commercialized_media < 3 : 我们的媒体建设得还不够。 ?] [? if resources < 1: 我们没有足够的资源。 ?]
on-arrival: workers_spd += 7*(1-dissent); new_middle_spd += 6*(1-dissent); unemployed_spd += 6*(1-dissent); pro_republic += 5*(1-dissent); resources -= 1; socialism += 5; capital_propaganda = 1

We launch a counter-propaganda campaign to blame the economic crisis on the capitalists, using all of our media channels to do so. Because of our existing media framework, this is moderately successful in directing popular criticisms towards the capitalists and away from us.


@business
title: 会见工商界领袖,设法把他们争取回来。
choose-if: reformist_strength > left_strength+center_strength and dvp_relation >= 30 and reconcile_business >= 1
unavailable-subtitle: [? if reformist_strength < left_strength+center_strength : 党内多数不支持这一温和举措。 ?][? if dvp_relation < 35 : 我们与企业界没有联系。 ?][? if reconcile_business < 1 : 这与我们应对危机的策略相悖。 ?]
on-arrival: reconcile_business += 1; economic_growth += 0.3; unemployed -= 0.2; inflation += 0.2; bourgeois_power += 0.02; socialism -= (5*bourgeois_power); coup_progress -= 1; coalition_dissent -= 1; dvp_relation += 3; reformist_strength += 15; left_dissent += 10; reformist_dissent -= 10; bourgeoisie_dnvp *= 0.9; bourgeoisie_nsdap *= 0.9; bourgeoisie_dvp += 5; capital_reconcile = 1

We meet with several leaders from the business community to try and assuage their concerns. Though little progress is made, we think we have at least established contacts through which we might continue in the future.

@nothing
title: 我们无能为力。

There is nothing we can do that is within constitutional and legal bounds.
