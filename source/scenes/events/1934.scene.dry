title: 1934
subtitle: A new year
new-page: true
tags: event
priority: 2
view-if: year >= 1934 and month >= 1
max-visits: 1
on-arrival: older_republic = old_republic; old_republic = pro_republic; {!
if (Q.unemployed >= 20) {
    Q.unemployed -= 2;
    Q.inflation += 1;
}
if (Q.inflation < -5) {
    Q.unemployed -= 1;
    Q.inflation += 2;
}
if (Q.economic_growth > 2) {
    Q.budget += 1;
    if ((Q.upper_taxes_rates+Q.lower_tax_rates) >= 5) {
        Q.budget += 1;
        Q.economic_growth -= ((Q.upper_tax_rates+(Q.lower_tax_rates/2))/10);
    }
}
if (Q.pro_republic > 75 && Q.pro_republic > Q.older_republic) {
    Q.new_middle_ddp += (Q.pro_republic - Q.older_republic)
    Q.old_middle_ddp += ((Q.pro_republic - Q.older_republic)/2)
    Q.new_middle_dvp += ((Q.pro_republic - Q.older_republic)/2)
    Q.old_middle_dvp += (Q.pro_republic - Q.older_republic)
}
// window.dendryUI.dendryEngine.state.visits = {"1934":1}
!}

= 1934

新的一年开始了。[? if works_program = 0 : 经济萧条似乎已经触底,但仍有许多苦难。现在启动一项经济拯救计划还不算晚。 ?][? if works_program >= 1 and unemployment >= 15 : 我们已开始推行一项公共工程计划,但经济仍陷困境。复苏已经开始,但还需做更多努力。 ?][? if works_program >= 1 and unemployment < 15 : 我们的公共工程计划成功地带领我们度过了危机最严重的阶段。经济正在好转,民众终于对未来有了乐观情绪。 ?]

[? if return_to_normalcy: 随着局势回归常态,德国的政治版图正在变化。自由主义在中间阶层中重新抬头,而右翼的激进政党则在衰落。 ?]

[? if president = "Hindenburg" : 总统健康状况很差,恐怕活不过今年。 ?]
